#!/usr/bin/env node
/**
 * Full-flow retry with:
 *   - Fresh Document + attachment (previous one was consumed by the failed step 6)
 *   - Subject rendered via MergeData using template:_EMLQC sigil + FromTemplate: [Subject, Body]
 *   - Explicit non-empty Subject hand-fallback if template has no subject configured
 */
export {};
//# sourceMappingURL=debug-send-v2.d.ts.map